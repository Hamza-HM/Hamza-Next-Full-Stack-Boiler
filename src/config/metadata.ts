import type { Metadata } from 'next';

export const siteMetadata: Metadata = {
  title: 'Hamza Nextjs Boilerplate',
  description: 'Created to avoid my repetitive tasks on each project.',
  icons: [{ rel: 'icon', url: '/favicon.ico' }],
};
